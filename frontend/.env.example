# Backend (prefixo /api é adicionado em src/lib/api.ts). Sem barra no fim.
# Produção (Render): URL pública do serviço Web da API, ex. https://SEU-API.onrender.com
VITE_API_URL=http://localhost:3000

# Clerk (Dashboard → Configure → API Keys → Publishable key)
VITE_CLERK_PUBLISHABLE_KEY=pk_test_...

VITE_ENV=development
